#ifndef _implementation
#define _implementation

/*
Function getHTML takes a string url and writes the HTML code to a string outfilename, which is the name of the output file
tested: works, returns html to an output file
*/
int getHTML(const char*, const char*);

#endif
